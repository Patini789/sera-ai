# SeraAI Dependencies
# Python 3.10+ required
# Install with: pip install -r requirements.txt
# For CUDA/GPU support, install PyTorch separately first:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# ── Core AI / ML ──────────────────────────────────────────
openai==1.99.6
numpy==2.2.6
pandas==2.2.2
requests==2.32.3
python-dotenv==1.0.1

# ── Embeddings & Whisper (STT) ────────────────────────────
faster-whisper==1.2.0
ctranslate2==4.4.0
tokenizers==0.22.1
huggingface-hub==0.24.6
onnxruntime-gpu==1.22.0

# ── Text-to-Speech ────────────────────────────────────────
edge-tts==7.2.7
pydub==0.25.1
PyAudio==0.2.14

# ── Web UI (Flask + SSE) ─────────────────────────────────
Flask==3.1.3
Jinja2==3.1.6
Werkzeug==3.1.7

# ── Discord Bot ───────────────────────────────────────────
discord.py==2.7.1
PyNaCl==1.5.0

# ── Godot Bridge & System ────────────────────────────────
websockets==16.0
psutil==7.1.3
pywin32==311

# ── MCP (Model Context Protocol) ─────────────────────────
# Used for agentic tool calling via external MCP servers

# ── Networking / HTTP ─────────────────────────────────────
httpx==0.28.1
aiohttp==3.13.5
certifi==2024.8.30
urllib3==2.2.3
charset-normalizer==3.4.4
idna==3.7

# ── Serialization & Validation ───────────────────────────
pydantic==2.12.5
pydantic-settings==2.13.1
PyYAML==6.0.3
jsonschema==4.26.0

# ── Utilities ─────────────────────────────────────────────
tqdm==4.66.4
coloredlogs==15.0.1
Pillow==11.3.0
typing_extensions==4.15.0
packaging==24.1
six==1.16.0
